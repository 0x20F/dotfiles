#!/usr/bin/env ruby



updates = `checkupdates`.lines.length()

# No output if there are no updates
if updates == 0
  exit(1)
end


# Output the label that polybar should show
print "#{updates} 󰏗"


